Run the code with a gcc compiler.i.e "gcc -o malware malware1.c"
Then Run "./malware1"
The project ask you to enter a file that you would like to open.Enter the file.
By the CONCEPT of pointers, the project is able to read the file without a password if needed!!
